task printGreeting {
    doLast {
        println greeting
    }
}

ext {
    // this will shadow all configuration
    // from build.properties or command line
    greeting = "Hello from inside the build script"
}